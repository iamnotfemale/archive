# Postgres connection (Neon via Vercel Marketplace sets DATABASE_URL automatically).
# Leave unset locally to use the JSON file store at .data/items.json.
DATABASE_URL=

# Write key. Leave unset locally for open writes.
# In production set a long random string, then visit https://<your-site>/key?t=<the-string> once per browser.
ARCHIVE_TOKEN=
